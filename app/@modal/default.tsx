// Requerido por Next.js para parallel routes: qué renderizar en el slot @modal
// cuando la ruta activa no lo intercepta (navegación directa, refresh, etc.).
export default function Default() {
  return null;
}
